1º Añadir la gestión de Usuarios (Administrador)

borrar usuarios si no tiene pedidos pendientes





SELECT DISTINCT p.id
FROM pedidos p, lineas_pedidos l
WHERE p.id=l.pedido_id
AND p.usuario_id = 7





























		<?php $categorias = Utils::showCategorias(); ?>
		<select name="categoria">
			<?php while ($cat = $categorias->fetch_object()): ?>
				<option value="<?= $cat->id ?>" <?=isset($usu) && is_object($usu) && $cat->id == $usu->categoria_id ? 'selected' : ''; ?>>
					<?= $cat->nombre ?>
				</option>
			<?php endwhile; ?>
		</select>
